services:
  db:
    image: postgres:15-alpine
    container_name: loteria_db
    user: root
    environment:
      POSTGRES_DB: ${DB_NAME:-loteria}
      POSTGRES_USER: ${DB_USER:-loteria_user}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-loteria_pass}
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-loteria_user}"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - loteria_network
    restart: unless-stopped

  web:
    build: .
    container_name: loteria_web
    command: >
      sh -c "python manage.py collectstatic --noinput &&
      gunicorn --bind 0.0.0.0:8000 --workers 4 --access-logfile /app/logs/access.log --error-logfile /app/logs/error.log --log-level info config.wsgi:application"
    ports:
      - "8000:8000"
    env_file:
      - .env.example
    environment:
      - DB_HOST=db
    depends_on:
      db:
        condition: service_healthy
    volumes:
      - static_volume:/app/static
      - images_volume:/app/media
      - logs_volume:/app/logs
    networks:
      - loteria_network
    restart: unless-stopped

volumes:
  postgres_data:
  static_volume:
  images_volume:
  logs_volume:

networks:
  loteria_network:
    driver: bridge
